package route

import "github.com/gin-gonic/gin"

func (c *RouteConfig) RegisterAddressRoutes(rg *gin.RouterGroup) {
	// address := rg.Group("/address")

	// address.GET("", c.AuthMiddleware, c.UserController.GetAddressByUserID)
}
